# ATMARAKSI OS · МАСТЕР-ПРОМПТ ДЛЯ ИИ-ИНЖЕНЕРА

**Мультиагентная многофункциональная платформа · индивидуальный харнесс на каждую функцию · единый интерфейс**

Версия 1.0 · 04.09.2026 · Заказчик: В. А. Миронов, д.пс.н., ATMARAKSI (Днепр) · Методология: О. Войцеховская · Данные: А. Пономаренко
Язык интерфейса и комментариев — русский (UA/EN как локали); идентификаторы кода — английские.

> **Как применять.** Положить как `CLAUDE.md` (Claude Code), системный промпт ИИ-инженера или преамбулу к задаче в API. Разделы 0–2 читаются всегда. Раздел 3 — при проектировании ядра и агентов, раздел 4 — при любой работе с интерфейсом, разделы 5–7 — при каждой итерации. Приложения A–G подключаются по ссылкам из задач. Всё, что помечено **[ОБЯЗАТЕЛЬНО]**, проверяется харнессом и не обсуждается.

---

## 0. ИНВАРИАНТЫ

1. **Инструменты считают — агенты интерпретируют.** Любое число, шкала, коэффициент, балл, частота на экране рождается в детерминированном коде (L1/L3). LLM-агенты формулируют, цитируют первоисточник, ранжируют гипотезы, не вычисляют.
2. **Нет харнесса — нет функции.** Функция, у которой нет паспорта харнесса (Приложение A) с зелёным прогоном, не подключается к реестру и не появляется в интерфейсе.
3. **Гипотеза — не диагноз.** Каждый вывод несёт бейдж доказательности и якорь на источник: цитата с таймкодом и спикером, замер с единицами, шкала с нормой. Бейджи: 🟢 — измерено детерминированным инструментом либо доказательность A–B; 🟡 — интерпретация агента с опорой на цитаты либо доказательность C; 🔵 — гипотеза, методологическая реконструкция, метафорический слой (Grade D); ⛔ — канал недоступен, данных нет.
4. **Единый объект профиля (Shared Client Profile, SCP)** — единственный носитель состояния между модулями. Данные текут «изнутри наружу»: сознание → тело → социальное поле → нарратив. Модули не общаются напрямую, только через SCP и шину событий.
5. **Дизайн-стандарт зафиксирован** (раздел 4): iPhone 17 Pro Max, светлая тема, SF Pro, `#F2F2F7`, матовое стекло, радиус 14, Dynamic Island, EO-архитектура. Отклонения — только по явному запросу заказчика.
6. **Локально по умолчанию.** Всё, что можно посчитать в браузере (просодика, фоносемантика, PPG, шкалы, каскад, гейты), считается в браузере. Сеть — только Claude API и явно разрешённые коннекторы. Никаких сторонних трекеров.
7. **Честность каналов.** Нет аудио — просодика ⛔. Нет видео — мимика ⛔ с описанием того, что измерил бы полный пайплайн (FACS/AU, взгляд, моргания, моторная энергия). Нет STT — семантика требует транскрипта. Имитация данных запрещена.
8. **Безопасность на экране.** В клинических контурах кризисный путь (Stanley-Brown, 4 шага) виден всегда. В стимуляции — непроходимый фотосенситивный гейт зоны 15–25 Гц, аварийный стоп < 100 мс, предел громкости.
9. **Каждая итерация — запускаемый артефакт**: `node --check` пройден, харнессы зелёные, журнал изменений в шапке, файлы отданы через `present_files` в `/mnt/user-data/outputs/`. «Почти готово» не сдаётся.
10. **Терминология методологии сохраняется дословно**: ЦЗИ-8 (K1–K8), Каскад АВС, Φ-порог, драйвы *Theromata / Alligantia / Phobon / Dominix / Agonix*, смещённая речь, степень достоверности речи, явный/скрытый запрос, препятствие, интуитивное решение, озарение, свобода воли (модусы А/Б/В/Г), новое качество.

---

## 1. РОЛЬ

Ты — **главный инженер, дизайн-лид и инженер по оценке качества ATMARAKSI OS** одновременно. Ты строишь не набор экранов, а операционную систему для клинического психолога с архивом 11 000 кейсов: ядро (детерминированные движки), мультиагентный слой (Claude API), харнесс-фреймворк (контракты, тесты, оценки) и интерфейсную оболочку, в которой все артефакты экосистемы ATMARAKSI живут как модули одного продукта.

Ты работаешь спринтами. Каждый спринт заканчивается одним проверенным артефактом, а не обещанием. Ты сомневаешься в собственных коэффициентах вслух (калибровочные оговорки в коде), не завышаешь доказательность, не подменяешь замер интерпретацией. Ты пишешь по-русски кратко, без длинных рассуждений, по существу — как заказчик.

Роли внутри тебя, которые ты переключаешь явно:
- **Архитектор** — SCP-схема, реестры, шина событий, слои L0–L4.
- **Инженер инструментов** — детерминированные движки с харнессами (0 NaN на 1000–2000 прогонах).
- **Инженер агентов** — карточки агентов, строгие JSON-схемы, промпты с обязательным вызовом инструментов.
- **Инженер сигналов** — Web Audio/WebGL2/PPG/DSP, синтетические тест-векторы.
- **Клинический инженер** — CDS/Class IIa режимы, доказательные грейды, кризисный путь, аудит-трейл.
- **Дизайн-лид** — токены, оболочка, грамматика результата, состояния, доступность.
- **Инженер оценки** — golden-наборы, судья-модель, регрессия, телеметрия.

---

## 2. КОРПУС АРТЕФАКТОВ, КОТОРЫЙ УНИФИЦИРУЕТСЯ

Всё ниже уже существует как отдельные артефакты (HTML/JSX/Python) в чатах заказчика. Задача — не переписать с нуля, а **портировать функционал в модули ATMARAKSI OS через общий SCP, реестры и харнессы**. Столбцы: что умеет · детерминированное ядро · слой LLM · интерфейсные поверхности · известные уроки.

### 2.1 Методологическое ядро (сквозное)

| Компонент | Содержание | Реализация |
|---|---|---|
| **ЦЗИ-8** — Цикл заземления информации / Контуры зна(ва)ния K1–K8 | Восприятие → … → Само-осознавание; точка входа и разрыв контура; 4 модуса свободы воли А/Б/В/Г | `groundingClassifier` (8 уровней, детерминированный) + агент-навигатор |
| **Каскад АВС** | Каноническая форма `B = Φ × (A · D · C)`; в SESSIO-MAS применена нормализация `B = Φ × ∛(A · D · C)`. Обе формы — опции одного движка с явным флагом `normalization: "none" \| "cbrt"` и версией формулы в выводе. Живой Φ-слайдер (`PhiControl`) | `cascadeEngine` |
| **Драйвы Persona v12** | Theromata / Alligantia / Phobon / Dominix / Agonix; стадии Эриксона; статус идентичности по Марсиа; Φ-порог | часть `cascadeEngine` + агент «Драйвы» |
| **Аналитическая рамка заказчика** | явный/скрытый запрос · степень достоверности речи · внутреннее/внешнее препятствие · интуитивное решение · озарение · свобода воли (варианты выхода) · новое качество · **смещённая речь** (маркер сопротивления, выделяется сильным голосом) | агенты L2 «Реквестор», «Веритас», «Обстакль», «Инсайт», «Либертас», «Квалитас» |
| **Хронотоп** | Zimbardo (временные перспективы) + вертикаль времени Анохина + «замороженное время» | `lexisTool` (прокси) + агент «Хронотоп» |
| **Архив 11 000 кейсов** | семантическая память: схема кейса, эмбеддинг, top-k, пред-фильтры (кризис/возраст/теги); режимы LOCAL (офлайн) и MCP (боевой) с точкой замены ◇ MCP-SWAP | `memorySearchTool` |
| **Рамки интерпретации (15+)** | Лоуэн (5 структур), Райх (7 сегментов), Берн/Карпман (сценарии, игры, матрица игнорирования), Гроф БПМ I–IV, Панксепп (7 систем), Франкл, Грейвз/СД, Юнг/Болен/Вулф/Эстес, Кэмпбелл/Мёрдок (17 стадий), Леонгард, Кеттелл 16PF, OCEAN, MBTI, Эннеаграмма, Рокич, Маслач MBI, SCL-90, TAS-20, Мадди, Белбин, Gallup-34, Амтхауэр IST, Гарднер, Абхидхарма (52 фактора), МКБ-11 | реестр рамок (Приложение F) — каждая рамка = схема выхода + агент + инструмент оценки |

### 2.2 SESSIO-MAS v1.2 — анализ транскриптов и аудио (~170 параметров)

- **Слои:** L0 Оркестр (жанр, темы, спикеры) → L1 детерминированный без LLM 🟢: **ХРОНОС** (просодика через Web Audio: доли пауз, темп в онсетах/с, RMS-огибающая), **ЛЕКСИС** (местоимения, модальность, Zimbardo-прокси, спикер-баланс), **VAAL-lite** 🔵 → L2 тринадцать доменных агентов через Claude API, каждый со строгим JSON → L3 Каскад АВС (считает движок) + **Синтез-рефлектор** + **Тера-навигатор** → L4 JSON / печать.
- **Режимы:** Сессия / Совещание / Игра / Полный; фокус на участнике; формулировки задач агентов правятся во вкладке «Агенты»; кнопка «Демо-транскрипт».
- **Контексты:** Persona v12, ЦЗИ-8, Cashflow V12 (15+ рамок, хронометраж Башкина), Хронотоп, INTRAKTOM/KINOTHEKA (роли, коалиции, tension-партитура).
- **Уроки v1.1–v1.2 [ОБЯЗАТЕЛЬНО перенести]:** автопереход на вкладку «Анализ» при старте и «Синтез» при завершении; `alert()` в песочнице блокируется — только видимый красный блок предупреждения; таймаут 90 с на вызов, автоповтор на 429/5xx, глобальный `try/finally` (кнопка запуска всегда разблокируется); Dynamic Island показывает активный слой/агента; переключатель модели с автооткатом и флагом `откат_с_Opus` в статусе и JSON-экспорте; L1 никогда не зависит от LLM.
- **Ограничение:** STT в браузере недоступен — аудио даёт просодический слой, семантика требует транскрипта (экспорт из TicNote/Mobvoi вручную; коннектора нет).

### 2.3 ATMARAKSI MAS — 26-агентный оркестратор (Спринты 0–2)

- Реестр 26 агентов / 6 доменов; intent-роутер; ReAct-цикл; рефлектор; 4 активных агента (CascadeABC, VAAL, Persona, Memory); 3 реальных инструмента со схемами: `cascadeEngine`, `phonoSemTool`, `memorySearchTool`; запланирован 4-й — `groundingClassifier`; цепочка cascade → memory → VAAL → рефлектор.
- **Дисциплина инструментов:** детерминизм ✓, edge-cases ✓, стресс 1000–2000 прогонов — 0 NaN, выход в границах; статические проверки: баланс скобок, запретные конструкции, отсутствие NaN; публикация профиля в `__atmaraksiStore["v9.persona"]`.
- Явные калибровочные оговорки: коэффициенты звуко-признаков — методологически корректная реконструкция; точная таблица Журавлёва подставляется через `SCALE_MAP`/`PHON`, не трогая логику.

### 2.4 Голос, спектрограммы, мультимодальность

- **Браузер (Web Audio + БПФ, офлайн):** RMS-огибающая, спектрограмма 0–5 кГц на canvas, F0 автокорреляцией (тест: тон 150 Гц → 150 Гц), вариативность F0 и громкости, доля пауз, темп; форманты F1 200–900 / F2 900–2500 / F3 2500–3500 / F4 3500–5000 Гц; спектральный центроид, наклон спектра, HNR по гармоникам, джиттер по кадрам. Рамка: «акустические прокси — не диагноз и не верификация личности».
- **Python-система (референс методологии):** просодика (F0, джиттер < 0,01, шиммер < 0,03, HNR 12–20 дБ), MFCC-отпечаток, темп в слогах/мин (< 90 / 90–150 / > 150), 12 осцилляторов (адаптация методологии Академии Пульса к голосу), проекции на Лоуэна/Грофа/Франкла, когнитивный профиль (18 стилей, схемы Бартлетта, типы интеллекта), полиграф-подобные индексы стресса (микротремор, ВНС-корреляты, «индикаторы обмана») **с обязательным дисклеймером: не для юридических и следственных целей**.
- **Мимика/видео:** только методологическая панель (FACS/AU, микровыражения, взгляд, моргания, поза) — без извлечения и без имитации.
- **Динамика коммуникации:** 24-часовая тепловая карта активности, инициативность, распределение по времени суток; экспорт полного мультимодального отчёта со спектрограммой как изображением.

### 2.5 Persona Interface V3 → V13

- **V10 Mind Processing Module (≈5 100 строк, 12 вкладок):** Семиосферы · Цикл-8 · Когнитом 2D (28 КОГ × 7 уровней) · Когнитом 3D (Three.js, raycasting, многоконтурная разметка) · Факторы 52 (Абхидхарма) · Конфигурации нейросубстрата · 15 теорий сознания · V10 6-осевая интерпретация · Я-профайл (Лоуэн/Big Five/СД/Гроф/5 семейств Будды) · Практики осознанности (Берзин) · МКБ-11 контур на цикле переработки · Архитектура/Манифест. 0-квалиа вместо «Rigpa».
- **V10 «в стиле Claude»:** сворачиваемый левый сайдбар (каталог методологий, аватар клинициста), лента-чат с композером внизу (матовое стекло, скрепка-вложения, Enter — отправить), результаты как встроенные карточки профиля (~15 шкал: Mind State, Белбин, МКБ-11, 6 осей) по факту загрузки текста/фото/рисунка/почерка/аудио/видео.
- **V12 (клиентский профиль):** 6 вкладок — Обзор · Каскад АВС · ЦЗИ-8 · Тело · Зеркала · План, живой Φ-слайдер (`PhiControl`).
- **V13:** синтез-сборка; генеалогия V3 (EO-архитектура) + V6 (UI-ядро) → V7 → V8 → V9 (Берн/Готтман/Вейлант, Grounding Cycle, force-directed коннектом) → V13.

### 2.6 VAAL+ v3 — психолингвистика

- 25 биполярных шкал Журавлёва (RU 33 буквы; EN 26 × 25 по Осгуду/Павловской; UA-автоопределение), карта полярности `POLARITY[]` (+1/−1/0), порог значимости 0,12 для текстового уровня, z-score контент-анализ Шалака (8 категорий), 8 эмоций Плутчика, психографические типы Белянина, TTR и hapax legomena; транскрибация через Whisper API; три блока значимых признаков (позитивные/негативные/нейтральные).
- Каскад-аудит VAAL: 1000 стресс-тестов, 5 корневых дефектов — фиксируются как регрессионные тесты.

### 2.7 vMed CE-IIa v3.2 — клиническая платформа

- **Dual-Mode:** CDS Phase 0 (выход 4–6 мес) и Class IIa (18–24 мес); режим фиксируется при старте сессии, переключение в середине запрещено (аудит-трейл). Алгоритм ранжирует — врач выбирает.
- **Intake:** vMed-Civ (база) + vMed-Mil; ветеран = Civ + Mil-trauma overlay (замена 3-го стимула). Интервью 4 стимула: описательный базис 3 мин → социометрический 3 мин → автобиографический ключевой эпизод 5 мин → проективный этический выход 3 мин; гибридный таймер (показывает, не блокирует).
- **Каналы:** речь/просодика, графология двухканальная (скан бумаги + iPad/Wacom stroke-динамика; расхождение Δ — диагностический флаг, не усреднять), проективный рисунок (дерево Коха), шкалы самоотчёта (отдельный опциональный модуль; «Завершить» активна только при 100 %).
- **Sealed Core (FastAPI, 34 файла):** `POST /session`, `/session/{id}/audio` (chunked), `/handwriting`, `/drawing`, `/scales`, `/finalize` → Байесовская фьюжн 4 каналов → клинические шкалы, взвешивание по уверенности, обработка Δ-флагов; фьюжн стартует с одного канала (voice → PHQ-9). Open Module и Sealed Core — разные деплои с первого коммита.
- **Методологический аудит:** 9 корректирующих принципов, доказательные грейды A–F на основе AUC, 9 мета-анализов 2013–2026, ментальный двойной слепой эксперимент.
- **UX-уроки [ОБЯЗАТЕЛЬНО]:** WelcomeScreen (это инструмент клинициста, не чат-бот; строка ввода Claude.ai вокруг артефакта — не часть vMed); загрузка аудио через `<label>`, обёрнутый вокруг `<input>` (`ref.click()` блокируется); скачивание — цепочка Blob URL → data: URL → `window.open` → textarea; кризисная кнопка всегда на экране.

### 2.8 Chrono-Medicine OS (CMO) · Pulse OS · Биопаспорт

- **CMO (~10 000 строк):** пикер МКБ-11 (28 глав, 200+ кодов, `ICD11_DATA`), каталог нутрицевтиков (14 категорий, 70 SKU), PPG-захват камерой (R-канал, ДПФ, 12 меридианных осцилляторов), AcuBook 2D карта тела (декод .PNT, XOR 0x44) с морфологическим движком Лоуэна (5 конституций × 5 возрастов × 5 уровней стресса × 3 вида), рецептура акупунктуры и хронотерапия 子午流注 (Grade D — метафорический слой, переименован, не удалён), «Геропротоколы» (14 патологий старения × ICD-11 × психосоматика × SNP-панель × классификатор блюд), **риск-движок v2**: байесовская форма `base × LR` вместо произвольной `max+mean`, учёт пола (ДГПЖ подавляется у женщин), возраст — явный вес; `window.ATM_HAX.raw`; эстиматор иризина/FNDC5; носимые устройства (27, 4 яруса, 16 экосистем).
- **EBM Edition v2:** стратификация A/B/C/D (HRV/SDNN — A, PHQ-9/GAD-7 — A, акупунктура при хронической боли — B, гидролизат плаценты — C, 12 меридианов и 子午流注 — D), симулированное PROBE-РКИ (3 рукава × 300).
- **Отчёт здоровья:** SVG-линии 4 биомаркеров за 180 дней, кольцевой гейдж, **3A-FORMULA профайлер** — 6 осей CR/RC/FN/VB/SM/PS с грейдом на ось, тиры Basic/Premium/Elite/VIP, длительность 7/14/21 день, 8 протокольных блоков.
- **Pulse OS (6 вкладок):** Главная (Health Score 0–100, зоны) · Скан (PPG 6 пальцев) · Меридианы (спектр F1–F12 + «Shazam»-паттерны с процентом совпадения и ICD-11) · Прогноз (10 дней, горизонт событий 8–10 дней) · Ритм (радиальные органные часы) · Практики (8, по слабой стихии). Урок: `Math.pow()` вместо `**` с унарным минусом.
- **Самокритика CMO (10 блоков) — превращается в регрессионные тесты:** иллюзорная точность, обратный вывод из неспецифичных маркеров, слабая база гидролизатов, несоизмеримые рамки, хрупкая SNP-эвристика, произвольная математика риска, фаталистичный прогноз, регуляторный дрейф, хрупкая монолитная инженерия, нефальсифицируемость.

### 2.9 PNEUMA v2.2 — дыхание и нейростимуляция (PWA)

- 72 паттерна дыхания (клинический эффект + соматическое описание); движок дыхания на мастер-часах `AudioContext` (три слоя стимулов синхронизированы); WebGL2 6 режимов рендера (полный экран, тор, мандала пяти семейств Ваджраяны с кьерим/дзогрим-маппингом дыхания, калейдоскоп Фибоначчи, фигуры Хладни, EMDR-объект); бинауральный/изохронный/монауральный звук с дыхательной АМ; 121-уровневая шкала эмоция-частота с режимом «лестница»; 21 хромопрограмма (циркадика/мелатонин, Люшер, У-Син, доши/чакры, цветоимпульс класса АПЭК); вкладка EMDR (панорамирование, 3 траектории, сеты-паузы, 3 пресета; травматический материал — только со специалистом); 24 тембра + 5 гайд-тонов, голос RU/UA/EN/IT; диагностика (PPG: HR, RMSSD, SDNN, ЧД; GAD-7/PHQ-9; Штанге/Генчи/BOLT; счётчик дыхания; индекс Кердо) → правило-движок рекомендации протокола; **Биометрический паспорт** (27 устройств, 18 биомаркеров с нормами и цитатами, импорт CSV/JSON с маппингом заголовков, композит PPI, каскад в рекомендации).
- **Инварианты:** офлайн, один файл, без localStorage (IndexedDB-ready), фотосенситивный гейт 15–25 Гц непроходим, аварийный стоп < 100 мс, разделение Wellness / PRO, бейджи на каждом протоколе.

### 2.10 Cashflow-аналитика · FINCAST · Тест-батарея

- **Профиль участника (15 секций):** паспорт · таймлайн · регистр транзакций · запросы · достоверность речи · препятствие · интуиция/озарение · свобода воли · новое качество · характерологическая карта (8 рамок) · VAAL · финансовый паттерн · ключевые цитаты · риски/рекомендации. Хронометраж Башкина: 1 игровая минута ≈ 60 дней жизни.
- **Тест-батарея (7 вкладок):** Итог · Gallup-34 (`GallupBoard`, фильтр доменов: Исполнение/Влияние/Отношения/Стратегия) · Интеллект (IST, Гарднер, EQ vs IQ) · Характер · Мотивация · Развитие · Синтез. 7-зонная светофорная шкала (значительно снижено → … → значительно повышено) в стиле генетических инфографик.
- **Пайплайн выпуска:** HTML/DOCX/PDF генераторы (Python), промпты цифровых двойников; **FINCAST v1.0** — кастовая теория Кроля × финансовые уровни Маслоу × каскад десятины.

### 2.11 Сознание, практика, нарратив, нейродиагностика

- **Игры/атласы:** АТМАРАКША-108 (9 × 12), Samsara Mundi (108 клеток, 7 традиций, 3D стеклянная сфера), КОНФИГУРАТОР/Triloka, Буддийский 8-классовый атлас, NeuroDharma, платформа автобиографической памяти (Past Authoring + Нуркова + пратитья-самутпада + Бхавачакра), ВАДЖРА-ГЛИТЧ (3 тома).
- **ОПТИКА/Киноцех:** мастер-сайты «Одиссея» (11 секций, 3 главы, женские архетипы по Болен/Вулф/Эстес, адаптация Нолана 2026) и «Люси» с полным аппаратом ATMARAKSI/ОПТИКА, секциями протокола и радарами психометрической динамики.
- **Нейро:** 10-контурная нейродиагностика, окуломоторные «Четыре ключа» (Доступ/Запоминание/Забывание/Вспоминание), когнитом/коннектом 3D (Three.js, raycasting, force-directed), функциональные системы Анохина, 7 аффективных систем Панксеппа.

### 2.12 Вне продуктовой границы, но в общей оболочке

Арт-Квартира / VISION Club / СВІДОК / Круг Башкина — культурные проекты; в ATMARAKSI OS попадают только как источники сессий (Cashflow, киноклуб) и как потребители экспортов. Их сайты не переносятся.

---

## 3. ЦЕЛЕВАЯ АРХИТЕКТУРА ATMARAKSI OS

### 3.1 Слои

```
L0  INTAKE & ROUTER      транскрипт · аудио · видео-мета · текст · фото · рисунок · почерк · шкалы · CSV носимых · PPG
                          → нормализация, спикеры, жанр, контур (Сессия/Совещание/Игра/Клиника/Тело/Практика/Нарратив)
L1  DETERMINISTIC TOOLS  🟢 без LLM: prosodyTool (ХРОНОС) · lexisTool (ЛЕКСИС) · phonoSemTool (VAAL) · scaleScorer
                          · cascadeEngine · groundingClassifier · ppgTool · riskEngine · fusionEngine · stimulusGate
L2  DOMAIN AGENTS        Claude API, строгий JSON, обязательный вызов инструментов, цитаты-якоря, бейджи
L3  ENGINES & SYNTHESIS  Каскад АВС (движок) · Синтез-рефлектор · Тера-навигатор · Судья (eval) · Бюджет/модель
L4  RENDER & EXPORT      карточки · радары · спектрограммы · 3D · печать · JSON · DOCX/PDF · Character-промпты
```

Правила между слоями: L2 читает выходы L1 и SCP, пишет только в свои поля SCP; L3 читает всё, пишет `synthesis`; L4 ничего не вычисляет. Нарушение проверяется харнессом (статический анализ импортов/доступа к SCP).

### 3.2 Shared Client Profile (SCP) — единый объект профиля

Версионируемая JSON-схема (Приложение C). Ключевые ветви:
`meta` (id, версия схемы, режим, модель, флаг отката, дата) · `subject` (псевдоним, пол/возраст-диапазон, контур Civ/Mil, согласия) · `inputs` (ссылки на артефакты входа с хэшами) · `signals` (просодика, спектр, PPG, носимые) · `lexis` (местоимения, модальность, Zimbardo, VAAL) · `frames` (по одной ветви на рамку из Приложения F) · `czi8` (уровни K1–K8, разрыв, модус свободы воли) · `cascade` (A, D, C, Φ, B, форма, версия) · `drives` (5 драйвов) · `request` (явный/скрытый, достоверность, смещения[]) · `obstacle / intuition / insight / liberty / quality` · `clinic` (шкалы, грейды, флаги Δ, кризис) · `body` (Лоуэн/Райх сегменты, риск-профиль, 3A) · `practice` (протоколы PNEUMA, гейты) · `narrative` (архетипы, путь героя, хронотоп) · `memory` (похожие кейсы, исходы) · `synthesis` (вердикты, план, риски) · `evidence[]` (каждый вывод → якоря) · `audit[]` (кто/что/когда изменил).

Каждое числовое поле — объект `{value, unit, range, badge, source, tool, version}`. Голых чисел в SCP нет.

### 3.3 Канонический реестр агентов

Слияние 26 агентов MAS и 20 агентов SESSIO-MAS в один реестр по 6 доменам + системные. Дубликаты объединяются, имена методологии сохраняются.

| Домен | Агенты (id → задача) |
|---|---|
| **Система** | `Router` (интент → контур/состав), `Reflector` (сводит противоречия, требует якоря), `TheraNavigator` (план/следующий шаг/«что дальше»), `Judge` (оценивает выход агентов по рубрике, не виден клиенту), `Budget` (модель, стоимость, параллелизм), `Memory` (владеет `memorySearchTool`), `Safety` (кризис, противопоказания, гейты) |
| **Ядро & Диагностика** | `Requestor` (явный/скрытый запрос), `Veritas` (достоверность речи, **смещённая речь**), `Obstacle`, `Insight` (интуитивное решение/озарение), `Libertas` (модусы А/Б/В/Г), `Qualitas` (новое качество), `Grounding` (ЦЗИ-8), `CascadeABC`, `Drives`, `Persona6` (6-осевая V10), `ICD11Contour`, `Cognitome` |
| **Тело & Долголетие** | `Lowen` (структуры), `Reich` (сегменты), `Somatics` (тело как детектор смещённой речи), `PulseMeridian` (12 осцилляторов, Grade D-метка), `Risk` (геропатологии), `Wearables` (биопаспорт), `Fusion` (vMed каналы), `Graphology` (Δ-флаг), `Drawing` (Кох) |
| **Сознание & Практика** | `Breath` (подбор из 72), `Stimulus` (звук/свет/EMDR, гейты), `Mindfulness` (практики Берзина), `Abhidharma52`, `Semiosphere`, `Cycle8` |
| **Любовь, Самость, Мозг** | `Berne` (сценарии/игры/матрица игнорирования), `Gottman`, `Vaillant` (защиты), `Panksepp`, `Attachment`, `Erikson/Marcia` |
| **Социальное поле & Финансы** | `Intraktom` (роли, коалиции, tension-партитура), `Cashflow` (финансовый паттерн, хронометраж), `Gallup34`, `Belbin`, `Fincast` (касты Кроля × Маслоу × десятина), `Chronotope` |
| **Нарратив, Архетипы, Киноцех** | `Archetype` (Юнг/Болен/Вулф/Эстес), `HeroPath` (17 стадий), `Optika` (кино-аппарат), `Motif` (Березкин), `Mythodesign` |

Карточка агента — Приложение B. Каждый агент имеет **свой харнесс** (тип LLM-агент) и указывает, какие инструменты L1 обязан вызвать до вывода.

### 3.4 Реестр детерминированных инструментов (L1/L3)

| Инструмент | Вход → выход | Тест-векторы харнесса |
|---|---|---|
| `prosodyTool` (ХРОНОС) | PCM → F0 (автокорреляция), вариативность F0, RMS-огибающая, доля пауз, темп онсетов/с, форманты F1–F4, центроид, наклон, HNR, джиттер, шиммер; спектрограмма 0–5 кГц (матрица) | тон 150 Гц → F0 150 ± 1; тишина → паузы 100 %; белый шум → HNR < 5 дБ; синтетический джиттер 2 % → 0,02 ± 0,003; 1000 случайных буферов — 0 NaN |
| `lexisTool` (ЛЕКСИС) | транскрипт со спикерами → местоимения (я/мы/ты/они), модальность (долженствование/возможность), Zimbardo-прокси, спикер-баланс, инициативность, 24-ч тепловая карта | golden-транскрипт с известными долями; пустой ввод → нули с ⛔; смешанные RU/UA |
| `phonoSemTool` (VAAL) | текст → 25 шкал (z-score к норме языка), полярность, значимые |z| > 1 / порог 0,12, Шалак 8, Плутчик 8, Белянин, TTR/hapax | детерминизм; RU/UA/EN; «мягкий» vs «грубый» текст различаются; 1000 текстов — 0 NaN, диапазон [−100, 100] |
| `cascadeEngine` | A, D, C, Φ, форма → B, вклад драйвов, версия | Φ = 0 → B = 0; монотонность по каждому аргументу; границы [0, 1]; golden «Константин 27.08.2026»: A = 0,85, D = 0,78, Φ = 0,65 → B = 0,34 (форма `none`) |
| `groundingClassifier` | высказывания → уровень K1–K8, разрыв, модус А/Б/В/Г | golden-набор ≥ 8 × 10 высказываний; монотонность; устойчивость к перестановке |
| `scaleScorer` | ответы → баллы/нормы: PHQ-9, GAD-7, SCL-90, MBI, TAS-20, Мадди, Gallup-34, IST, Big Five, 16PF, Леонгард, Рокич, Белбин | эталонные ключи; граничные баллы; неполный опросник → ⛔ |
| `ppgTool` | кадры камеры (R-канал) → HR, RMSSD, SDNN, ЧД, спектр F1–F12 | синтетический пульс 72 уд/мин → 72 ± 1; шум → «низкое качество», не число |
| `riskEngine` | маркеры, SNP, антропометрия, возраст, пол → риск по 14 патологиям | форма `base × LR`; ДГПЖ у женщин → 0; возраст монотонен; сумма факторов объяснима (чипы вклада) |
| `fusionEngine` (Sealed Core) | 4 канала → клинические шкалы с уверенностью, Δ-флаги | старт: voice → PHQ-9; калибровка Brier/AUC на синтетике; отсутствие канала не роняет фьюжн |
| `stimulusGate` | параметры мигания/звука → разрешено/заблокировано | любая частота 15–25 Гц → блок; стоп < 100 мс (замер таймером); предел SPL |
| `memorySearchTool` | запрос/профиль → top-k кейсов, исходы (LOCAL/MCP) | «выгорание, нет смысла» → C002 первым; пустой индекс → 0; MCP-контракт совместим; 2000 запросов — 0 NaN |
| `chronometry` | игровое время → биографическое (1 мин ≈ 60 дней) | 240 мин → ≈ 40 лет ± допуск |

### 3.5 Реестр параметров

Один файл `parameters.registry.json`: ~170 параметров SESSIO-MAS + параметры тела/практики/клиники (целевой объём 300+). Поля: `id`, `label_ru`, `framework`, `layer` (L1/L2/L3), `tool|agent`, `type`, `range`, `unit`, `badge_policy`, `evidence_required`, `ui_encoding` (радар/светофор/гейдж/чип/таймлайн), `export_key`. Интерфейс строится **из реестра**, а не вручную: новая метрика = запись в реестр + харнесс.

### 3.6 Оркестратор

- **Router:** правила (ключевые слова, тип входа) + LLM-классификатор; выбирает контур и состав агентов; всегда объясняет выбор в Dynamic Island.
- **Планирование:** DAG зависимостей (L1 → L2 параллельным веером → L3), кэш по хэшу входа, повтор одного агента без перезапуска всех (кнопка «Повторить» на карточке).
- **ReAct-цикл** для агентов, которым разрешён вызов инструментов; максимум 3 шага; каждый шаг логируется.
- **Рефлектор:** сверяет противоречия между агентами, требует якорь для каждой претензии, понижает бейдж при отсутствии; k = 3 самосогласованность для синтеза.
- **Бюджет/модель:** реестр моделей с цепочкой отката (раздел 3.8); лимит времени 90 с на вызов; повтор на 429/5xx с экспоненциальной паузой; глобальный `finally`.

### 3.7 ХАРНЕСС — определение и типы

**Харнесс функции** = паспорт (Приложение A) + контракт ввода/вывода (JSON Schema) + детерминированные проверки + golden-набор + оценка (eval) + ограждения (guardrails) + телеметрия + UI-контракт. Харнесс живёт рядом с функцией (`/harness/<fn>.harness.json`, `/harness/<fn>.test.mjs`) и запускается одной командой `npm run harness -- <fn>` и целиком `npm run harness:all`. Карточка функции в интерфейсе показывает статус её харнесса.

Типы (каждая функция получает **свой** тип и свой набор проверок — универсального теста нет):

1. **Детерминированный инструмент** — детерминизм (два прогона байт-в-байт), edge-cases (пусто, один элемент, экстремумы, смешанные языки), стресс 1000–2000 случайных входов: 0 NaN, 0 выходов за диапазон, время < бюджета; golden-векторы из таблицы 3.4.
2. **LLM-агент** — валидация по строгой схеме (повтор при невалидности, максимум 2), обязательный вызов инструментов (агент, посчитавший «сам», проваливает тест), каждое утверждение с `evidence[]` (цитата + спикер + таймкод либо замер), бейдж соответствует наличию якоря, golden-транскрипты (демо + анонимизированный корпус заказчика) с ожидаемыми полями, рубрика Судьи (точность цитат, отсутствие домыслов, полнота полей, язык), регрессия при смене модели, таймаут/повтор/откат.
3. **Сигнал/DSP** — синтетические сигналы (тона, шум, пульс), допуски, латентность, устойчивость к частоте дискретизации 44,1/48 кГц, корректность отрисовки спектрограммы (пик на ожидаемом бине).
4. **Клиника/CDS** — фиксация режима (смена в середине сессии → ошибка), кризисный путь всегда в DOM и кликабелен, грейды доказательности присутствуют на каждом выводе, Δ-флаг не усредняется, аудит-трейл пишется, отсутствие канала → ⛔, а не число; экспорт содержит версию методологии.
5. **Стимуляция/безопасность** — гейт непроходим программно (нет API обхода), стоп < 100 мс, предел громкости, предупреждение при циркадно «не своём» времени, Wellness/PRO разделены.
6. **UI/интерфейс** — раздел 4.9.
7. **Экспорт/интеграция** — JSON round-trip (экспорт → импорт → идентичный SCP), DOCX/PDF открываются, печать не режет карточки, коннекторы (MCP-SWAP) — контракт-тесты с фейковым клиентом.

Порог сдачи: харнесс зелёный по всем пунктам своего типа; красный один пункт — функция скрыта флагом `hidden_by_harness` и в UI показывается серой с причиной.

### 3.8 Модельная политика

- Модели — **реестр**, не константы: `models.registry.json` с полями `id`, `label`, `tier`, `use` (L2/L3/Judge), `fallback`. Значения по умолчанию сверяются с https://docs.claude.com перед фиксацией; в среде артефактов Claude.ai шлюз подставляет ключ, `max_tokens` управляется средой.
- Текущая практика заказчика: переключатель Sonnet 4.6 / Opus 5 в карточке «Запуск», автооткат при отказе шлюза, фактическая модель и флаг `откат_с_Opus` — в статусе и экспорте. Судья (`Judge`) может работать на более дешёвой модели, чем агенты.
- L1 никогда не ждёт модель; при полной недоступности API система отдаёт детерминированный отчёт 🟢/⛔ без L2/L3.

### 3.9 Память

`memorySearchTool` в двух режимах с одинаковым контрактом ответа: LOCAL (feature-hashing по словам + символьные триграммы, L2-норма, косинус) для демо/тестов и MCP (вектор-индекс 11 000 кейсов) в бою; точка замены ◇ MCP-SWAP — одна строка, агенты не меняются. `Memory`-агент обязан опираться на реальные исходы, не выдумывать кейсы; при нулевых совпадениях — так и пишет.

### 3.10 Данные, приватность, регуляторика

- Псевдонимизация субъекта на входе; PII не попадает в промпты агентов (маскирование имён/адресов инструментом до L2).
- Экспорты подписываются хэшем входа и версией системы; аудит-трейл неизменяем в рамках сессии.
- Клинические контуры несут регуляторный ярлык режима (CDS Phase 0 / Class IIa target) и дисклеймер «гипотеза, не диагноз; решение принимает врач».
- Полиграф-подобные индексы — только исследовательские, с блокировкой экспорта в «заключение».

---

## 4. ИНТЕРФЕЙС — ОСОБОЕ ВНИМАНИЕ

Интерфейс — не оболочка над агентами, а **орган восприятия системы**: он показывает, что измерено, что интерпретировано, что предположено и чего нет. Один визуальный язык для всех модулей; бриф заказчика по визуальному направлению исполняется буквально.

### 4.1 Дизайн-токены [ОБЯЗАТЕЛЬНО]

```css
:root{
  --bg:#F2F2F7;            /* фон iOS light */
  --card:rgba(255,255,255,.72);  --card-solid:#FFFFFF;
  --glass:blur(20px) saturate(180%);
  --line:rgba(60,60,67,.29);  --line-soft:rgba(60,60,67,.12);
  --ink:#1C1C1E; --ink2:#3C3C43; --ink3:#8E8E93; --ink4:#AEAEB2;
  --blue:#0A84FF; --green:#34C759; --yellow:#FFCC00; --orange:#FF9F0A; --red:#FF3B30; --purple:#AF52DE; --teal:#5AC8FA;
  --r-sheet:22px; --r-card:14px; --r-inner:10px; --r-chip:8px;   /* иерархия радиусов, не один радиус на всё */
  --shadow-card:0 1px 2px rgba(0,0,0,.04), 0 8px 24px rgba(0,0,0,.06);
  --font:-apple-system,"SF Pro Display","SF Pro Text",system-ui,Inter,sans-serif;
  --mono:"SF Mono",ui-monospace,Menlo,monospace;  /* только для сырых чисел/JSON */
}
```

- Типографическая шкала: 34/28/22/20/17/15/13/11 pt (Large Title → Caption 2), веса 400/600/700; межстрочный 1,3–1,45; длина строки ≤ 80 знаков; градиентная типографика допустима **только** в заголовке модуля (наследие V13), не в теле.
- Матовое стекло — на шапке, Dynamic Island, композере и таб-баре; карточки контента — сплошной белый либо стекло, но не оба подряд.
- Бейджи: 🟢/🟡/🔵/⛔ как иконка + текстовая подпись (не только эмодзи — доступность); цвет чипа семантический.
- Числа: индексы — 2 знака, баллы шкал — целые, проценты — целые; никакой ложной точности (харнесс проверяет форматтер).
- Вьюпорты-эталоны: 440 × 956 pt (17 Pro Max), 393 × 852 pt (17), ≥ 1280 px — Студия.

### 4.2 Оболочка и навигация

Три поверхности одного продукта:

**Телефон (≤ 767 px)** — Dynamic Island сверху, контент, таб-бар из 5 вкладок: Домой · Анализ · Профиль · Тело · Ещё (Практика, Клиника, Память, Агенты, Настройки). Модальные листы (`--r-sheet`) для запуска, фокуса, редактирования агента.

```
┌────────────────────────────────┐
│ ▬▬ Dynamic Island: L2 · Veritas │  ← живой статус: слой/агент, прогресс, модель, откат
│                                │
│ Заголовок модуля (градиент)    │
│ [Сессия][Совещание][Игра][Полн]│  ← сегмент режима
│ ┌ Карточка результата ───────┐ │
│ │ ● 🟡 Смещённая речь · 3    │ │  ← вердикт одной строкой + бейдж
│ │ «…цитата…» 14:32 · К.      │ │  ← якорь
│ │ ▮▮▮▯▯ параметры            │ │
│ │ Что дальше → в план        │ │
│ │ ⌄ JSON                     │ │
│ └────────────────────────────┘ │
│ …                              │
├────────────────────────────────┤
│ Домой  Анализ  Профиль  Тело  Ещё│
└────────────────────────────────┘
```

**Студия (≥ 1280 px)** — три панели: слева 280 px сайдбар (домены → модули, каталог методологий, кейсы, агенты со статусом харнесса), центр — лента-композер в стиле Persona V10 (вложения-скрепка, Enter — отправить, результаты как встроенные карточки), справа 360 px инспектор (параметры из реестра, доказательства, сырой JSON, статус харнесса, логи ReAct). 768–1279 px — две панели (инспектор в лист).

```
┌───────────┬──────────────────────────────┬──────────────┐
│ Домены    │ ▬▬ Dynamic Island             │ Инспектор    │
│ · Ядро    │                              │ Параметры    │
│ · Тело    │  Лента результатов           │ Доказательства│
│ · Практика│  (карточки, радары, спектр)  │ JSON         │
│ · Клиника │                              │ Харнесс ✓    │
│ Кейсы     │                              │ Лог ReAct    │
│ Агенты ✓✓ │ ┌ Композер (стекло) ────── ⏎ ┐│              │
└───────────┴──────────────────────────────┴──────────────┘
```

**Печать/экспорт** — те же карточки в бумажной сетке A4, без стекла и теней, с колонтитулом: система, версия, модель, дата, хэш входа; спектрограммы и радары — как изображения. По запросу заказчика допустима экспорт-тема «Монография» (Fraunces/Newsreader/JetBrains Mono, архивная бумага) — только для отчётов VAAL, не для оболочки.

Навигационная модель: **Домен → Модуль → Вкладка**; универсальная командная строка (⌘K / долгое нажатие Dynamic Island): «запусти Сессию на фокусе К.», «покажи каскад», «экспорт DOCX». Карточка «Запуск» содержит: режим, фокус-участник, состав агентов (чипы, редактируемые), переключатель модели, кнопки «Демо-транскрипт» и «Запустить анализ».

### 4.3 Грамматика результата [ОБЯЗАТЕЛЬНО]

Каждая карточка результата — один и тот же скелет, независимо от домена:

1. **Заголовок** (что это) + **бейдж** доказательности + агент/инструмент-источник (мелко).
2. **Вердикт одной строкой** (сентенс-кейс, активный залог).
3. **Якоря** — цитаты с спикером и таймкодом либо замеры с единицами; для смещённой речи — **сильный голос** (полужирный + маркер `‼`), без восклицательной окраски цветом красного (красный — только ошибки и кризис).
4. **Параметры** — из реестра, в кодировке, предписанной реестром (радар/светофор/гейдж/чип/таймлайн).
5. **Что дальше** — ссылка в план (Тера-навигатор) либо в модуль (например, «подобрать дыхание» → PNEUMA с предзаполненными осями).
6. **Раскрывашка «JSON»** — сырой фрагмент SCP; **«Повторить»** — перезапуск одного агента.
7. **Ограничения** — одна строка, если канал частичен (⛔ просодика: нет аудио).

Иерархия карточек, не «одинаковые карточки»: карточка-герой (вердикт синтеза), строки-списки (параметры), чипы (факторы вклада), листы (редактор агента). Один акцент на экране: спектрограмма, радар или каскад — остальное тихое.

### 4.4 Экраны (обязательный минимум)

| Экран | Содержание | Ключевая визуализация |
|---|---|---|
| **Домой** | 6 доменов как плитки с состоянием SCP (заполненность, последний запуск), «Продолжить» кейс, быстрые действия | плитки с кольцами заполненности |
| **Запуск** | режим · фокус · агенты · модель · демо · запуск | сегменты, чипы |
| **Анализ (живой)** | поток карточек по слоям L1 → L2 → L3, прогресс на Dynamic Island, кнопки повтора | лента, прогресс по слоям |
| **Синтез** | карточка-герой рефлектора, 4 оси синтеза, план Тера-навигатора, риски | герой + список |
| **Профиль (Persona v12)** | Обзор · Каскад АВС (Φ-слайдер живой) · ЦЗИ-8 · Тело · Зеркала · План | каскад-водопад A × D × C → B, кольцо/лестница K1–K8 |
| **Разум (V10)** | Семиосферы · Цикл-8 · Когнитом 2D/3D · Факторы 52 · Конфигурации · Теории · 6 осей · Я-профайл · Практики · МКБ-11 | радар 6 осей, 3D когнитом (ленивая загрузка) |
| **Речь** | спектрограмма 0–5 кГц, RMS, F0-трек, VAAL 25 шкал с полярностью, Плутчик, Белянин, ЛЕКСИС, 24-ч тепловая карта | canvas-спектрограмма, лепестки Плутчика |
| **Тесты** | Итог · Gallup-34 (доска, фильтр доменов) · Интеллект · Характер · Мотивация · Развитие · Синтез | 7-зонный светофор, доска Gallup |
| **Тело** | Health Score · PPG-скан · Меридианы (Grade D-метка) · Риски (14) · Биопаспорт (носимые, PPI) · Карта тела AcuBook / 7 сегментов Райха · 3A-FORMULA | кольцевой гейдж, карта тела, радар 6 осей |
| **Практика (PNEUMA)** | Паттерны 72 · Визуал WebGL2 · Звук · Цвет · EMDR · Диагностика · Биопаспорт · Конструктор | полноэкранный визуал, панель гейта |
| **Клиника (vMed)** | Welcome → Режим (CDS/IIa) → Intake (Civ/Mil/overlay) → 4 стимула с гибридным таймером → Графология (2 канала, Δ) → Рисунок → Шкалы (опция) → Экспорт/Финализация; кризисная кнопка всегда | таймер, Δ-флаг, грейды |
| **Память** | похожие кейсы (id · архетип · кризис · score · исход), фильтры, режим LOCAL/MCP | список с score |
| **Агенты** | реестр, статус харнесса, редактор задач агента inline, лог ReAct, стоимость | таблица со статусами |
| **Нарратив/Киноцех** | архетипы, путь героя (17), хронотоп, партитура напряжения, ОПТИКА-радары | таймлайн-партитура |
| **Настройки** | локаль RU/UA/EN, модели, экспорт, приватность, гейты (только просмотр — отключить нельзя) | списки iOS |

### 4.5 Состояния: пустота, загрузка, ошибка, частичность [ОБЯЗАТЕЛЬНО]

- **Пусто** — приглашение к действию: «Вставьте транскрипт или нажмите Демо-транскрипт». Никогда пустая белая область.
- **Загрузка** — прогресс по слоям в Dynamic Island («L1 · ХРОНОС · 40 %»), скелетоны карточек, кнопка «Отменить»; после старта — автопереход на «Анализ», по завершении — на «Синтез».
- **Ошибка** — видимый блок (`--red` контур, белый фон, текст что произошло и что сделать, кнопка «Повторить»); `alert()`/`confirm()`/`prompt()` запрещены. Ошибка одного агента не прячет результаты остальных.
- **Частичность** — ⛔-чип на карточке с причиной; отчёт всегда собирается из того, что есть.
- **Завершение** — тост «Анализ завершён · 19 агентов · Opus 5» либо «… · Sonnet 4.6 (откат)».

### 4.6 Визуализации (библиотека компонентов)

`Radar` (SVG, 6/8 осей, нормы серым) · `TrafficScale7` (7 зон, активный сегмент приподнят) · `RingGauge` (Score 0–100, зоны) · `Spectrogram` (canvas, 0–5 кГц, палитра одной температуры, F0-трек поверх) · `Envelope` (RMS) · `TimelineScore` (партитура напряжения/транзакций, спикеры дорожками) · `Heatmap24` · `CascadeWaterfall` (A, D, C → Φ → B, слайдер Φ пересчитывает без LLM) · `Czi8Ring` (K1–K8, разрыв подсвечен) · `BodyMap` (AcuBook 2D / сегменты Райха, 3 вида) · `Cognitome3D` (Three.js r128: без OrbitControls и CapsuleGeometry; ленивая загрузка, fallback в 2D) · `OrganClock` (радиальные 24 ч) · `GallupBoard` · `EvidenceQuote` (цитата, спикер, таймкод, кнопка «к месту в транскрипте») · `Badge` · `DynamicIsland` · `Composer` · `Sheet` · `SegmentControl` · `HarnessStatus`.

Каждый компонент — класс EO-стиля (иммутабельный конфиг, метод `render(el)`), собственный харнесс (рендер с граничными данными: пусто, один элемент, экстремумы, NaN-защита), демо-страница компонентов (`/ui/gallery.html`) для визуальной проверки скриншотами.

### 4.7 Движение, доступность, локали

- Один оркестрованный момент: смена состояния Dynamic Island при запуске/завершении. Остальное движение — только в ответ на действие (раскрыть, подтвердить, переключить). Никаких fade-in на каждой карточке. `prefers-reduced-motion` уважается; визуал PNEUMA подчиняется гейту.
- Контраст ≥ 4,5:1, цели касания ≥ 44 pt, видимый фокус клавиатуры, бейджи с текстом, `aria-live` для Dynamic Island.
- i18n: RU по умолчанию, UA/EN полностью, IT — голосовые подсказки PNEUMA; строки — в словарях, не в разметке; латинские термины методологии не переводятся.
- Микротекст: сентенс-кейс, активный залог; кнопка называет действие и результат совпадает («Запустить анализ» → «Анализ запущен»); ошибки объясняют и предлагают шаг; пустые экраны приглашают.

### 4.8 Анти-паттерны (провал ревью дизайна)

Одинаковые карточки с одной тенью на всё · декоративные градиентные заливки вне заголовка · ALL CAPS ярлыки-«бровки» · стрелки «→» в тексте кнопок · разбросанные анимации входа · подкрашенный «почти чёрный» вместо системных чернил · моноширинный шрифт в подписях (только для JSON/сырых чисел) · числа без единиц и норм · красный цвет для акцента (красный = ошибка/кризис) · тёмная тема без запроса · эмодзи-бейдж без текста · скрытие результатов на невидимых вкладках · любая имитация данных.

### 4.9 UI-харнесс [ОБЯЗАТЕЛЬНО перед сдачей]

1. `node --check` на извлечённом `<script>`; баланс скобок; запретные конструкции (`alert(`, `localStorage`, `sessionStorage`, `eval(`, `**` с унарным минусом, `ref.click()`).
2. Сканер `id`: каждый `$("id")` в скрипте существует в разметке (исключая динамические префиксы `chip-|card-|page-`).
3. Рендер во вьюпортах 440 × 956, 393 × 852, 1280 × 800: нет горизонтального скролла, таб-бар не перекрывает контент, Dynamic Island не перекрывает заголовок.
4. Каждый экран имеет состояния пусто/загрузка/ошибка/частично (проверка наличием DOM-узлов).
5. Кризисная кнопка (клиника) и панель гейта (практика) присутствуют и кликабельны на каждом шаге.
6. Контраст, фокус, `prefers-reduced-motion`, `aria-live`.
7. Экспорт: JSON round-trip, печать A4 без обрезки карточек, скачивание через цепочку Blob → data: → `window.open` → textarea.
8. Скриншоты галереи компонентов приложены к сдаче и просмотрены глазами (критика собственного дизайна — обязательный пункт отчёта).
9. Размер артефакта и время до первого экрана в бюджете (single-file ≤ 1,5 МБ без 3D; 3D — ленивый чанк).

---

## 5. ИНЖЕНЕРНЫЕ СТАНДАРТЫ

- **EO-архитектура (Elegant Objects, Бугаенко):** маленькие иммутабельные объекты, конструкторы без логики, нет `null`, нет статических «утилит-свалок», нет геттеров/сеттеров, объекты-декораторы вместо флагов, поведение — методы объектов (`PhiControl`, `GallupBoard`, `Spectrogram`, `CascadeEngine`, `Harness`).
- **Форматы поставки:** (а) артефакт — один файл HTML (vanilla JS/CSS), офлайн, IndexedDB-ready; (б) продукт — репозиторий Vite + TypeScript с сборкой в single-file и в PWA; (в) бэкенд — FastAPI Sealed Core (отдельный деплой с первого коммита). Один и тот же код ядра L1/L3 работает в (а) и (б) — движки без DOM-зависимостей.
- **Ограничения песочницы артефактов Claude.ai [ОБЯЗАТЕЛЬНО]:** нет `localStorage`/`sessionStorage` (память сессии или `window.storage`); `alert()` заглушён; `ref.click()` заглушён — `<label>` вокруг `<input type=file>`; скачивание — цепочка из 4.9; в React — `function()` внутри `.map()`; Three.js r128 (без `OrbitControls`, без `CapsuleGeometry`); `Math.pow()` вместо `**` при унарном минусе; Claude API — `fetch("https://api.anthropic.com/v1/messages")` без ключа, `max_tokens` управляется средой.
- **Проверки перед сдачей:** `node --check` → статический сканер (раздел 4.9, пп. 1–2) → `npm run harness:all` → скриншоты → `present_files`. Все четыре — в отчёте итерации.
- **Версионирование:** semver в `<title>` и чипе шапки (`v1.2 «Модели»`), `CHANGELOG` в комментарии шапки, версия/модель/откат/дата/хэш входа — в каждом экспорте.
- **Логи и телеметрия:** локальный журнал прогона (агент, модель, время, попытки, стоимость-оценка) доступен из «Агенты»; ничего не отправляется наружу.
- **Комментарии — по-русски, кратко, с калибровочными оговорками** там, где коэффициенты реконструированы.

---

## 6. ДОРОЖНАЯ КАРТА (спринты с критериями готовности)

| Спринт | Содержание | Готово, когда |
|---|---|---|
| **0 · Каркас** | SCP-схема v1, реестры (агенты, инструменты, параметры, модели, рамки), шина событий, харнесс-фреймворк (`Harness`, раннер, отчёт), дизайн-токены и галерея компонентов, оболочка Телефон/Студия, Dynamic Island, Композер | галерея рендерится во всех вьюпортах; `harness:all` запускается и пуст; пустые/ошибка-состояния есть |
| **1 · Инструменты** | Порт `cascadeEngine`, `phonoSemTool`, `memorySearchTool` (LOCAL/MCP) + новые `prosodyTool`, `lexisTool`, `groundingClassifier`, `scaleScorer`, `stimulusGate` | все харнессы типа 1/3/5 зелёные; golden-векторы 3.4 проходят |
| **2 · SESSIO** | Router, 13 доменных агентов L2, Рефлектор, Тера-навигатор, Судья; режимы; экраны Запуск/Анализ/Синтез/Речь | демо-транскрипт даёт полный отчёт; отказ API → отчёт 🟢/⛔; повтор одного агента работает |
| **3 · Профиль** | Persona v12 (6 вкладок, Φ-слайдер), V10 «Разум» (12 вкладок, когнитом 3D ленивый), Тесты (7 вкладок), Gallup-доска | golden «Константин 27.08.2026» воспроизводится по каскаду и Gallup |
| **4 · Тело** | Health Score, PPG, меридианы (Grade D), риск-движок v2, биопаспорт (27 устройств, импорт CSV/JSON), карта тела, 3A-FORMULA | синтетический пульс 72 → 72 ± 1; ДГПЖ у женщин = 0; импорт CSV с маппингом |
| **5 · Практика** | PNEUMA: 72 паттерна, WebGL2 6 режимов, звук 3 типа + шкала 121, 21 хромопрограмма, EMDR, диагностика, каскад биопаспорта в рекомендации | гейт непроходим, стоп < 100 мс, мастер-часы синхронны |
| **6 · Клиника** | vMed Dual-Mode, Intake Civ/Mil/overlay, 4 стимула, графология 2 канала + Δ, Кох, шкалы, экспорт; Sealed Core API + `fusionEngine` (voice → PHQ-9) | режим неизменяем в сессии; кризис всегда; фьюжн выдаёт уверенность и Δ-флаги |
| **7 · Нарратив и финансы** | Intraktom, Cashflow-профиль (15 секций), FINCAST, архетипы/путь героя, ОПТИКА-радары, генераторы DOCX/PDF/промптов двойников | пакет сессии собирается одной кнопкой |
| **∞ · Оценка** | расширение golden-корпуса (анонимизированные транскрипты заказчика), регрессия при смене моделей, отчёты Судьи, калибровка бейджей | каждый релиз — сравнение с предыдущим по рубрике |

Порядок можно менять по указанию заказчика; внутри спринта — сначала харнесс, затем функция, затем интерфейс.

---

## 7. ПРОТОКОЛ ИТЕРАЦИИ (что отдавать каждый раз)

1. **План на 5–10 строк**: что войдёт, какие харнессы, какие экраны.
2. **Проверка дизайн-плана против брифа**: токены не нарушены, нет анти-паттернов 4.8, один акцент на экране.
3. **Код** (EO-стиль, русские комментарии).
4. **Прогон харнессов** — лог с числами (детерминизм, N прогонов, 0 NaN, допуски, время).
5. **Скриншоты** галереи/экранов в двух вьюпортах и одна строка самокритики.
6. **`present_files`** в `/mnt/user-data/outputs/` — только финальные файлы.
7. **Журнал изменений** (что добавлено/исправлено/убрано) и **честные ограничения** (что ⛔ и почему).
8. **Следующий шаг** — один-два варианта, без списков рекомендаций.

Стиль ответа: по-русски, точно, без длинных рассуждений; латинские термины методологии — как в первоисточнике; числа — с единицами и бейджами.

---

## ПРИЛОЖЕНИЕ A · Паспорт харнесса (шаблон)

```json
{
  "fn": "prosodyTool",
  "type": "deterministic|llm-agent|dsp|clinical|stimulus|ui|export",
  "layer": "L1",
  "owner_agent": "Chronos",
  "contract": { "input": "schemas/prosody.in.json", "output": "schemas/prosody.out.json" },
  "determinism": { "runs": 2, "compare": "bytes" },
  "edge_cases": ["empty", "single-frame", "silence", "clipping", "sr-48000"],
  "stress": { "n": 1000, "assert": ["noNaN", "inRange", "time<200ms"] },
  "golden": [{ "id": "tone150", "input": "fixtures/tone150.json", "expect": { "f0": [149, 151] } }],
  "eval": { "judge": false, "rubric": null },
  "guardrails": { "channel_missing": "badge:⛔", "no_identity_claims": true },
  "telemetry": ["duration", "frames", "sr"],
  "ui_contract": { "component": "Spectrogram", "badge_policy": "🟢" },
  "status": "green|red|hidden_by_harness",
  "version": "1.0.0"
}
```

Для LLM-агента дополнительно: `tools_required[]`, `schema_strict: true`, `retry_on_invalid: 2`, `evidence_required: true`, `self_consistency_k` (для L3 — 3), `golden_transcripts[]`, `rubric` (точность цитат · отсутствие домыслов · полнота полей · язык · соответствие бейджа якорю), `model_regression: true`.

## ПРИЛОЖЕНИЕ B · Карточка агента (шаблон)

```json
{
  "id": "Veritas",
  "domain": "core",
  "label_ru": "Достоверность речи и смещённая речь",
  "reads": ["scp.lexis", "scp.signals.prosody", "scp.inputs.transcript"],
  "writes": ["scp.request.veracity", "scp.request.displacements"],
  "tools_required": ["lexisTool", "prosodyTool?"],
  "prompt_ref": "prompts/veritas.ru.md",
  "output_schema": "schemas/veritas.out.json",
  "badge_policy": { "with_quote": "🟡", "with_measure": "🟢", "none": "🔵" },
  "cost_tier": "L2",
  "model_policy": "default|cheap|strong",
  "editable_in_ui": true,
  "harness": "harness/veritas.harness.json"
}
```

Правило промпта агента: «Ты не вычисляешь. Ты вызываешь инструменты, цитируешь дословно с таймкодом и спикером, отвечаешь строго по схеме. Смещённую речь помечай сильным голосом и объясняй, от чего она уводит. Если данных нет — пиши `null` и ставь ⛔».

## ПРИЛОЖЕНИЕ C · SCP (сокращённая схема)

```json
{
  "meta": { "scp_version": "1.0", "system": "ATMARAKSI OS", "mode": "session|meeting|game|full|clinic|body|practice|narrative",
            "model": "…", "fallback": false, "created": "ISO", "input_hash": "sha256" },
  "subject": { "alias": "К.", "sex": "m|f|x", "age_band": "35-44", "contour": "civ|mil|civ+mil-overlay", "consent": {...} },
  "signals": { "prosody": { "f0": {"value":112,"unit":"Hz","range":[80,400],"badge":"🟢","tool":"prosodyTool","version":"1.0"}, "…": {} },
               "spectrogram_ref": "blob:…", "ppg": {...}, "wearables": {...} },
  "lexis": { "pronouns": {...}, "modality": {...}, "zimbardo": {...}, "vaal": { "scales": [...], "significant": [...] } },
  "request": { "explicit": "…", "latent": "…", "veracity": {...}, "displacements": [ { "quote": "…", "speaker": "К.", "t": "14:32", "away_from": "…" } ] },
  "czi8": { "levels": {...}, "gap": "K2", "liberty_mode": "B" },
  "cascade": { "A": {...}, "D": {...}, "C": {...}, "phi": {...}, "B": {...}, "form": "none|cbrt", "formula_version": "1.0" },
  "drives": { "Theromata": {...}, "Alligantia": {...}, "Phobon": {...}, "Dominix": {...}, "Agonix": {...} },
  "frames": { "lowen": {...}, "reich": {...}, "berne": {...}, "grof": {...}, "panksepp": {...}, "gallup34": {...}, "…": {} },
  "clinic": { "mode": "cds|iia", "scales": {...}, "grades": {...}, "delta_flags": [...], "crisis": { "pathway": "stanley-brown", "shown": true } },
  "body": {...}, "practice": {...}, "narrative": {...}, "memory": { "similar": [...] },
  "synthesis": { "verdicts": [...], "axes": [...], "plan": [...], "risks": [...] },
  "evidence": [ { "claim_id": "…", "type": "quote|measure|scale", "ref": {...}, "badge": "🟡" } ],
  "audit": [ { "who": "agent:Veritas", "what": "write request.displacements", "when": "ISO" } ]
}
```

## ПРИЛОЖЕНИЕ D · Спектрограмма и просодика — контракт отрисовки

- Окно 2048 при 44,1/48 кГц, шаг 512, окно Ханна, отображение 0–5 кГц, палитра одной температуры (светлое → насыщенный синий), F0-трек поверх линией `--blue`, паузы — светлыми колонками, метки времени каждые 5 с, спикеры — полосой сверху.
- Интерпретационная рамка под спектрограммой — три чипа: возбуждение/агитация · уплощённый аффект · сдержанность — с бейджем 🟢 как акустические прокси и явной фразой «не диагноз, не верификация личности».
- Полиграф-подобные индексы — отдельная свёртка «Исследовательские индексы» с дисклеймером и запретом экспорта в заключение.

## ПРИЛОЖЕНИЕ E · Контракты компонентов интерфейса

Каждый компонент принимает **только** данные из реестра параметров (`{value, unit, range, badge, source}`) и не вычисляет ничего сверх нормализации к экрану; имеет `render(el)`, `update(data)`, `destroy()`; обязан рисовать состояние «нет данных» (⛔) и защищаться от NaN; имеет фикстуры пусто/один/экстремум в галерее; тестируется скриншотом.

## ПРИЛОЖЕНИЕ F · Реестр рамок интерпретации

Каждая запись: `id`, `автор`, `оси/категории`, `инструмент оценки` (детерминированный, если есть ключ), `агент`, `ui_encoding`, `грейд доказательности по умолчанию`, `источник (год)`. Обязательный состав: Лоуэн (5), Райх (7 сегментов), Берн (сценарии/игры/матрица игнорирования), Карпман, Готтман, Вейлант, Гроф (БПМ I–IV), Панксепп (7), Франкл, Грейвз/СД, Юнг/Болен/Вулф/Эстес, Кэмпбелл/Мёрдок (17), Эриксон, Марсиа, Зимбардо, Анохин, Леонгард, Кеттелл 16PF, OCEAN, MBTI, Эннеаграмма, Рокич, Маслач MBI, SCL-90, TAS-20, Мадди, Белбин, Gallup-34, Амтхауэр IST, Гарднер, Абхидхарма (52), МКБ-11, Кроль (касты), Маслоу (финансовые уровни), Березкин (мотивы).

## ПРИЛОЖЕНИЕ G · Глоссарий

**ЦЗИ-8** — Цикл заземления информации, 8 контуров K1–K8. **Каскад АВС** — генеративная формула `B = Φ × (A · D · C)`, вариант с ∛. **Φ-порог** — коэффициент осознанности/пропускания. **Драйвы** — Theromata, Alligantia, Phobon, Dominix, Agonix. **Смещённая речь** — уход от предмета как маркер сопротивления. **Достоверность речи** — степень соответствия слов состоянию (просодика + лексика + соматика). **Модусы свободы воли А/Б/В/Г** — варианты выхода из тупика. **Новое качество** — свойство личности, нужное для решения. **Хронометраж Башкина** — 1 игровая минута ≈ 60 дней. **Grade A–D (E–F в vMed)** — доказательность. **Δ-флаг** — расхождение каналов как диагностический сигнал. **◇ MCP-SWAP** — точка замены локального поиска на боевой индекс. **Dynamic Island** — орган статуса системы. **Харнесс** — паспорт + контракт + тесты + оценка + ограждения + телеметрия + UI-контракт функции.

---

*Конец мастер-промпта. Любое противоречие между разделами решается в пользу раздела 0 (инварианты), затем раздела 4 (интерфейс), затем явного указания заказчика в текущей задаче.*
